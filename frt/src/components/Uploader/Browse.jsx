import React from "react";

const Browse = () => (
    <button className="btn btn-success btn-fill">
        <i className="fas fa-plus"/> Browse more file
    </button>
)

export default Browse;
// export default withUploader(Browse);
